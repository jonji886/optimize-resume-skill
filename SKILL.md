---
name: optimize-resume
description: 针对岗位 JD 与中文简历进行匹配诊断、事实补全、ATS 适配和精益改写，并生成或迭代更新“姓名-{岗位名称}.md”。用户要求简历优化、JD 匹配、改写现有简历、精简个人优势、筛选项目、检查夸大或调整简历结构时使用。
---

# 简历优化

以猎头筛选和业务面试官追问两个视角工作。优先保证事实可信、岗位相关和表达具体；不为提高匹配度编造经历、数据或 scope。

## 输入与资源

需要以下信息：

- 目标岗位 JD；
- 原始简历或现有目标简历；
- 用户在当前及前序对话中确认、否认或补充的事实。

缺少 JD 或简历且无法从上下文取得时，再向用户补齐。

按需读取：

- 判断不同岗位的内容取舍时，读取 references/role-mappings.md；
- 需要改写示例、scope 用词或面试钩子时，读取 references/examples.md。

## 先建立事实账本

改写前在内部整理四类事实：

1. **已确认**：用户明确提供，可直接写入；
2. **已否认**：用户明确说没有，禁止写入；
3. **未知**：没有证据，不进入最终简历；
4. **可迁移能力**：有相近底层经验，但必须明确边界，不包装成直接经验。

执行规则：

- 已否认事实优先级最高，后续版本不得重新带回；
- 不把个人 Demo 写成客户项目，不把技术支持写成客户成功，不把协作写成团队管理；
- 不补写未经确认的比例、金额、团队人数、客户结果、工作方式或空窗期解释；
- 工作年限按实际任职月份计算，不用“当前年份－毕业年份”替代；
- 最终文件中不得出现 TODO、scope 注释或其他内部标记。

## 判断工作模式

根据用户意图选择一种模式，不机械重复完整流程。

### 模式 A：仅诊断

用户要求分析、评估或指出问题时，只输出诊断，不改文件。

### 模式 B：首次优化且存在关键缺口

当缺失事实会实质改变岗位匹配或参与层级时，先诊断并提问：

1. 职业风险扫描；
2. JD 核心要求与职级范围；
3. ATS 的技能、scope、行业匹配；
4. 已匹配项与关键缺口；
5. 最多 5 个具体问题，注明补 ATS 证据还是面试钩子。

问题只覆盖高价值缺口。若没有阻断改写的缺口，不强制提问。

### 模式 C：信息足够或用户已回答

直接生成目标简历，不重复第一阶段。

### 模式 D：迭代修改现有简历

用户要求精简、删除、调整某个模块或套用新格式时，直接修改目标文件，同时检查相邻模块的一致性；不重新发起完整诊断。

## 职业风险扫描

首次优化时检查：

| 项目 | 参考阈值 | 处理方式 |
|---|---|---|
| 任职稳定性 | 连续 3 段平均少于 1.5 年 | 询问项目制、业务调整等真实背景 |
| 空窗期 | 最近 5 年内超过 6 个月 | 询问真实经历；未确认不得解释 |
| 职业路径 | 岗位方向发生明显变化 | 说明能力迁移，不直接判定为降级 |
| 行业连续性 | 3 年内切换 2 个以上不相关行业 | 提炼共通能力并弱化无关经历 |
| 学历与年限 | 不满足 JD 硬性条件 | 明确风险，不通过文案掩盖 |

将这些问题称为“需解释项”，避免把正常转型直接定义成硬伤。

## JD 拆解与证据映射

提取：

- 3 个核心硬技能；
- 2 个可行为验证的软素质；
- 职级与 scope 预期；
- 行业标签；
- 明确的必备项与加分项。

为每项要求寻找简历证据，内部标记为：

- **直接匹配**：同类岗位、客户或项目中做过；
- **可迁移**：底层能力相近，但场景不同；
- **无证据**：不写成已具备。

ATS 优先复现真实的技术名词、产品名、行业词和职责名词。不要为了原词复现堆砌动词，也不要把 JD 的“负责”机械改写成候选人的“主导”。

## 内容筛选

对每条经历内部评分：

- **2 分**：直接证明 JD 核心要求，保留并前置；
- **1 分**：证明可迁移能力，压缩为 1–2 条；
- **0 分**：与目标岗位无关，删除。

项目不强求数量。通常保留 1–3 个最相关项目；弱相关个人项目不得仅因包含 AI 关键词而占用版面。

个人优势与核心技能保持分工：

- 个人优势写“能力结论 + 证据或规模”，不复述完整工作过程；
- 核心技能写技术、工具和业务方法，不重复客户、金额和项目结果。

## 改写规则

### 真实性与 scope

按实际参与层级选词：

| 层级 | 含义 | 常用表达 |
|---|---|---|
| 独立承担 | 独自设计或完成 | 独立设计、搭建、编写 |
| 主导 | 跨团队推进并承担主要结果 | 主导、串联、打通、闭环 |
| 核心执行 | 承担关键模块 | 实现、集成、落地、定位 |
| 支持 | 提供局部或辅助工作 | 支持、协助、跟进、维护 |

“主导”整份简历通常不超过 3 次，但以事实为准。最终简历不展示 scope 标签。

### 语言

- 删除空洞形容词和没有对象的万能句，如“能力强”“经验丰富”“显著提升”；
- “负责、参与、优化、提升、推动”等词并非绝对禁用；只有缺少具体对象、动作和结果时才替换；
- 优先使用编写、拆解、串联、定位、抽象、打通、落地、验收等具体动词；
- 句子以一遍读懂为准，避免一条 bullet 同时塞入多个无关主题。

### 数据与结果

- 只使用用户确认的数据，并对关键数据加粗；
- 无数据时使用可验证的状态变化、覆盖范围、交付物或验收结果；
- 不要求每条个人优势都带数字；
- 技术项目没有商业数据时，可写测试结果、覆盖场景、上线状态或使用对象；
- 只有真实存在技术折中、风险或限制时才写，不强制制造“反过度包装”句。

### 面试钩子

整份简历保留 2–3 个真实、具体且候选人能展开的追问入口即可。钩子可来自：

- 兼容多套系统或历史数据；
- 多方接口边界；
- 关键选型理由；
- 上线后的异常与收场；
- 从项目定制抽象为标准能力。

不要为每个项目机械添加钩子。

## 输出结构

~~~markdown
# 姓名

手机/微信：xxx ｜邮箱：xxx

## 个人优势

- **关键字**：能力结论 + 证据或规模。

## 工作经历

### 公司｜职位

时间

- **关键字**：动作 + 场景 + 结果或价值。

## 项目经历

### 项目｜角色

时间

- **关键字**：动作 + 场景 + 结果或验证。

## 核心技能

- **类别**：技术、工具及应用场景。

## 教育背景与认证

- 学校｜专业｜学历｜时间
- 已确认的认证
~~~

格式规则：

- 顶部不写求职意向、工作经验、城市、性别、年龄或籍贯；
- 个人优势控制在 3–4 条，每条不超过 2 行；
- 工作经历每条必须使用“**关键字**：内容”，关键字通常为 4–8 个字且同一经历中不重复；
- 项目经历建议使用相同格式，背景说明可不加关键字；
- 工作经历按倒序排列，每段通常 3–6 条；
- 项目经历通常 1–3 个，每个 2–3 条；
- 核心技能独立；教育背景与认证合并；
- 输出文件命名为“姓名-{岗位名称}.md”，去除公司和城市前缀；
- 修改现有目标简历时更新原文件，不额外创建“最终版”“优化版”等重复文件。

## 交付前检查

1. 核对已否认和未知事实没有进入最终文件；
2. 核对 JD 前 3 个核心要求都有真实证据或明确标为可迁移；
3. 删除与 JD 无关的职责、项目和泛化工具；
4. 检查个人优势与工作经历、核心技能之间没有机械重复；
5. 检查参与层级、日期、工作年限和数据一致；
6. 检查顶部格式、章节顺序、关键字格式及所有内部注释；
7. 对 Markdown 文件运行：

   python3 scripts/lint_resume.py /绝对路径/姓名-{岗位名称}.md

脚本只能检查格式和重复，真实性仍需根据事实账本人工核对。

## 最终回复

提供目标文件的可点击路径，并简要说明：

- 本次保留和强化了什么；
- 删除或压缩了什么；
- 仍有哪些真实缺口需要面试准备。

只有实际设计了面试钩子时才说明钩子，不机械输出后续建议。
